# Morning Audit 2026-08-28

**Date:** 2026-08-28  
**Pages checked:** 78 HTML files (53 how-to articles + index, 7 areas, 8 services, 8 other root pages + 404)  
**New since last audit:** 3 how-to articles added by nightly script (hazel-hedge-care, box-hedge-alternatives, blackthorn-hedge-care)

---

## Auto-fixed

Nothing to fix. Site passed all auto-fix checks cleanly.

---

## Audit Results by Check

### 1. How-to inventory
All 53 how-to articles linked from `how-to/index.html`. No orphans. No broken links from the index. All 53 cards have a `data-howto-cat` attribute set. Categories all appropriate: timing, species, conifers, restoration, wildlife, law, planting, diy, pests. No mis-categorised entries found.

### 2. Internal links
Zero broken internal HTML links across all 78 pages.

### 3. Video embeds
25 YouTube watch URLs found across the how-to articles. **Cannot verify** — outbound proxy blocks YouTube (recurring issue; same as all previous audits). Flag carried forward.

### 4. SEO basics
All 78 pages pass:
- `<title>` present ✓
- Unique `<meta name="description">` present on every page, no duplicates ✓
- `<link rel="canonical">` present ✓
- `<meta name="theme-color" content="#14573a">` correct on every page ✓

### 5. JSON-LD
- `BreadcrumbList` present on all 78 pages ✓
- `index.html`: `LocalBusiness` + `WebSite` + `WebPage` + `BreadcrumbList` + `FAQPage` ✓
- All 53 how-to articles: `Article` + `BreadcrumbList` in `@graph` ✓

### 6. Sitemap
76 URLs total. All 54 how-to entries present (`how-to/` index + 53 articles). All services and areas pages listed. Every entry has a `<lastmod>` date. No missing entries.

### 7. Robots.txt
All required bots explicitly allowed:
GPTBot, ChatGPT-User, OAI-SearchBot, PerplexityBot, ClaudeBot, Claude-Web, Google-Extended, CCBot, Applebot, Applebot-Extended, Bingbot, Amazonbot, DuckAssistBot, Meta-ExternalAgent, YouBot ✓

### 8. Tone
- Em-dashes: none found across all 78 pages ✓
- Banned filler (utilise, leverage, seamlessly, best-in-class, robust, synergy, cutting-edge): none found ✓
- Non-UK spellings (organisation-level check): none found ✓

### 9. WCA-1981 nesting callout
43 of 53 how-to articles carry a Wildlife and Countryside Act 1981 nesting-season reference. The 10 articles without it are either non-timing topics (pest diagnosis, feeding, tool maintenance, planting comparisons) where the callout is not required, or the one carry-over below.

### 10. Content relevance
All 53 how-to articles are on-topic (hedge/tree care, Kent-specific). No off-topic content found.

---

## Needs Review

### 1. `how-to/choosing-hedge-species-kent.html` — missing WCA-1981 nesting callout (carry-over)
Article covers species selection with implicit timing guidance (e.g. "One cut a year" for beech, "Needs a trim after flowering" for escallonia) but does not include an explicit reference to the Wildlife and Countryside Act 1981 nesting window (1 March–31 August). Flagged in audits on 2026-08-25, 2026-08-26, and 2026-08-27. Needs new prose matching the house-style callout pattern used in `when-to-trim-hedges.html` and `wildlife-friendly-trimming.html`. **Content decision; do not auto-fix.**

### 2. YouTube video embeds — cannot verify (recurring)
25 YouTube watch URLs across the how-to articles. Outbound proxy blocks YouTube so HTTP status cannot be confirmed. This has appeared in every audit to date. Options: (a) accept the risk and monitor manually occasionally, (b) configure proxy allowlist for youtube.com HEAD requests, or (c) skip video-embed checks in the automated audit. **Infrastructure decision; report only.**
