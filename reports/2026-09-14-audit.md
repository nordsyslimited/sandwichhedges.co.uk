# Morning Audit — 2026-09-14

Auditor: automated morning audit  
Scope: 103 how-to pages + all services, areas, root, utility pages (total 128 HTML files checked, 126 indexable); sitemap, robots.txt, internal links, video embeds, JSON-LD, SEO, tone, legal callouts  
New articles this run: euonymus-hedge-care.html, hedge-noise-reduction-kent.html, formative-hedge-pruning.html (added by nightly content run 2026-09-14)

---

## Summary

**No auto-fixes applied.** All items in the auto-fix scope were already correct: every page has the correct theme-color, canonical, and meta description; no em-dashes found; no banned filler words in editorial text; BreadcrumbList present on all 128 pages; sitemap complete with lastmod on every entry; robots.txt allow-list complete.

**6 needs-review items** — 5 carry-overs from 2026-09-13, plus 1 new finding: two how-to index cards use space-separated `data-howto-cat` values instead of the comma-separated format the filter JS requires, meaning they do not appear under their secondary category tabs. This bug has been present since the 2026-09-12 nightly content run.

---

## Auto-fixed

None.

---

## Check-by-check results

### 1. How-To Inventory

**103 articles in filesystem. 103 linked from how-to/index.html. No orphans, no broken index links.**

Today's three new articles are correctly listed in the index with correct categories:
- `euonymus-hedge-care.html` → `data-howto-cat="species"` ✓
- `hedge-noise-reduction-kent.html` → `data-howto-cat="planting,wildlife"` ✓
- `formative-hedge-pruning.html` → `data-howto-cat="planting,timing"` ✓

**NEW (MEDIUM):** `elder-hedge-care.html` card in index has `data-howto-cat="species wildlife"` (space-separated) and `bamboo-screening-kent.html` has `data-howto-cat="species diy"` (space-separated). The filter JS splits on comma: `.split(',').map(s => s.trim())`, so these cards never appear under the "wildlife" or "diy" tabs respectively. The source comment explicitly states "comma-separated". Bug introduced in the 2026-09-12 nightly run; undetected by the 2026-09-12 and 2026-09-13 audits. Fix: change spaces to commas in those two `data-howto-cat` attributes.

**Carry-over (MEDIUM):** `box-blight-treatment.html` is indexed under `data-howto-cat="species"` only. The article covers a fungal disease; adding "pests" as a secondary category would be consistent with `box-tree-moth-caterpillar-damage.html`. Content decision required.

**Carry-over (LOW):** `pruning-an-overgrown-shrub-or-bush.html` shows howto-tag text "Comparison" in the card but has `data-howto-cat="diy"`. Filter works correctly; the visible pill label is cosmetically inconsistent with all other cards. Content decision.

### 2. Internal Links

**All clear.** Every relative and root-relative `href` checked across all 128 HTML files. No broken links. The root-relative links in `reducing-an-oversized-hedge.html` (`/services/hedge-trimming.html` etc.) resolve correctly in the production web-server context.

### 3. Video Embeds

**Proxy-blocked — carry-over (7th consecutive run).** All YouTube HEAD requests returned HTTP 000 (connection blocked at the outbound proxy). Cannot confirm liveness of any embedded video from this environment.

- **32 video IDs** identified across the site — some via `<iframe>` embeds, others via text-only links to YouTube (bamboo, elder, pittosporum, planting-bare-root-hedges, and others with channel-authority footnotes).
- No new video IDs were added by today's nightly content run (euonymus, hedge-noise-reduction, formative-hedge-pruning articles each contain no video embed).
- Resolution requires a network route permitting outbound YouTube requests.

### 4. SEO Basics

**All clear on all 128 pages, including new articles.**
- `<title>`: present on all pages ✓
- `meta name="description"`: present and unique on all 128 pages ✓
- `<link rel="canonical">`: present on all pages ✓
- `meta name="theme-color" content="#14573a"`: present on all pages including 404.html ✓

New articles:
- `euonymus-hedge-care.html`: all four pass ✓
- `hedge-noise-reduction-kent.html`: all four pass ✓
- `formative-hedge-pruning.html`: all four pass ✓

### 5. JSON-LD

**All clear.**
- BreadcrumbList: present on all 128 HTML files ✓
- Home page: `LocalBusiness` + `WebSite` + `WebPage` + `BreadcrumbList` + `FAQPage` in @graph ✓
- All 103 how-to articles: `Article` in @graph alongside `BreadcrumbList` ✓ (informational guides; `HowTo` not used, appropriate)
- New articles all have correctly structured @graph with `Article` + `BreadcrumbList` ✓

### 6. Sitemap

**All clear.** 103 how-to articles in sitemap; all services, areas, and root pages present. All `<lastmod>` dates present and populated.

New articles all have sitemap entries with `lastmod>2026-09-14`:
- `euonymus-hedge-care.html` ✓
- `hedge-noise-reduction-kent.html` ✓
- `formative-hedge-pruning.html` ✓

404.html and thanks.html are correctly excluded.

### 7. Robots.txt Allow-list

**All clear.** All 15 required bots present with `Allow: /`:  
GPTBot, ChatGPT-User, OAI-SearchBot, PerplexityBot, ClaudeBot, Claude-Web, Google-Extended, CCBot, Applebot, Applebot-Extended, Bingbot, Amazonbot, DuckAssistBot, Meta-ExternalAgent, YouBot ✓

### 8. Tone

**All clear.**
- Em-dashes: zero found across all 128 pages ✓
- Banned filler words: one match for `robust` in `bamboo-screening-kent.html` — this is inside `Fargesia robusta` (Latin botanical name), not editorial filler ✓
- Non-UK spellings: `color` and `center` appear only in HTML/CSS attributes and inline styles (not editorial prose) ✓

### 9. Legal Callouts — WCA 1981

**All clear on timing-related articles.** All articles that advise readers on when to trim reference the nesting-season window (1 March–31 August).

New articles checked:
- `euonymus-hedge-care.html`: timing content present — WCA nesting-season reference required and present ✓
- `hedge-noise-reduction-kent.html`: species/planting comparison, no trimming-timing advice — WCA callout not required ✓
- `formative-hedge-pruning.html`: timing content present — WCA nesting-season reference required and present ✓

**Carry-over (LOW):** `salt-tolerant-coastal-hedges-kent.html` references nesting-season rules by link to `when-to-trim-hedges.html` rather than an inline article-callout block. Functional but does not match the house callout pattern. Content decision.

### 10. Content Relevance

**All clear.** All three new articles (euonymus hedge care, hedges for noise reduction, formative pruning) are directly topically relevant to a Kent hedge and garden boundary service.

**Carry-over (LOW):** `formal-vs-informal-hedge.html` categorised under `data-howto-cat="species,planting"`. "planting" is accurate; "species" is a stretch for a style-choice guide. No new off-topic concern.

---

## Needs-Review Summary (6 items: 5 carry-over, 1 new)

| # | File | Issue | Severity | Status |
|---|------|-------|----------|--------|
| 1 | how-to/index.html | `elder-hedge-care` and `bamboo-screening-kent` cards have space-separated `data-howto-cat`; JS `.split(',')` means they don't appear under secondary filter tabs (wildlife, diy) | MEDIUM | **NEW — 1st run** |
| 2 | how-to/box-blight-treatment.html | `data-howto-cat` missing "pests" secondary category; article covers a fungal disease | MEDIUM | Carry-over (7th run) |
| 3 | 32 YouTube video IDs site-wide | Video liveness unverifiable (proxy-blocked, HTTP 000) | MEDIUM | Carry-over (7th run) |
| 4 | how-to/salt-tolerant-coastal-hedges-kent.html | WCA 1981 nesting-season referenced by link only, no inline article-callout block | LOW | Carry-over (5th run) |
| 5 | how-to/formal-vs-informal-hedge.html | `data-howto-cat="species"` is borderline for a style/preference guide | LOW | Carry-over (4th run) |
| 6 | how-to/pruning-an-overgrown-shrub-or-bush.html | howto-tag label "Comparison" does not match `data-howto-cat="diy"` | LOW | Carry-over (3rd run) |
